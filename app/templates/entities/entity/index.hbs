<div data-test-entity>
  {{@model.name}}
</div>

<ul>
  {{#each @model.properties as |property|}}
    <LinkTo @route="entities.entity.property" @model={{property.id}} data-test-property="{{property.id}}">{{property.name}}</LinkTo>
  {{/each}}
</ul>
